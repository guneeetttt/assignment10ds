#include <iostream>
#include <vector>
#include <unordered_set>
using namespace std;

int main()
{
    int n, m;
    cin >> n;
    vector<int> A(n);
    for (int i = 0; i < n; i++)
        cin >> A[i];

    cin >> m;
    vector<int> B(m);
    for (int i = 0; i < m; i++)
        cin >> B[i];

    unordered_set<int> s;
    for (int x : A)
        s.insert(x);

    unordered_set<int> common;
    for (int x : B)
    {
        if (s.find(x) != s.end())
            common.insert(x);
    }

    bool first = true;
    for (int x : common)
    {
        if (!first)
            cout << " ";
        cout << x;
        first = false;
    }

    return 0;
}
