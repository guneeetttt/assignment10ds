#include <iostream>
#include <vector>
#include <unordered_set>
using namespace std;

int main()
{
    int n;
    cin >> n;
    vector<int> nums(n);
    for (int i = 0; i < n; i++)
        cin >> nums[i];

    unordered_set<int> s;
    bool hasDup = false;
    for (int x : nums)
    {
        if (s.find(x) != s.end())
        {
            hasDup = true;
            break;
        }
        s.insert(x);
    }

    if (hasDup)
        cout << "true";
    else
        cout << "false";
    return 0;
}
